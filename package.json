{
  "name": "company-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "firebase": "^12.10.0",
    "mongodb": "^7.1.1",
    "react-router-dom": "^7.9.6"
  }
}
